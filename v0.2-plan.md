# Implementation plan v0.2 — granulat pe sesiuni Claude Code

## Premise consolidate

- Video = textură (sprite cu frame-uri pre-extrase). **Zero audio din video.**
- Audio doar din track-uri externe declarate explicit de user.
- Frame extraction: pre-extract PNG sequence (opțiunea A).
- `end > trimRange` → freeze ultimul frame. `loop: true` opțional, suportat din start.
- Register asset video: warn pe rezoluție/durată mari, nu eroare.

## Reminder critic — dual schema

`apps/editor/app/types/commands.ts` ține tipurile UI separat de engine. **Orice câmp nou pe scenă (audio track, video item) trebuie adăugat în AMBELE locuri** sau e silent stripped. Toate sesiunile de schema/MCP au asta ca acceptance criterion explicit.

---

## Sesiunile

### S1 — Schema + types: audio tracks
**Scope:**
- Tip nou `AudioTrack` în engine schema JSON: `asset, start, end?, volume?, fadeIn?, fadeOut?`
- Adăugare în `apps/editor/app/types/commands.ts` (DUAL!)
- Validări: `start ≥ 0`, `end > start`, `volume ∈ [0, 2]`, `fadeIn/fadeOut ≥ 0`
- Composition JSON capătă secțiunea `audio: AudioTrack[]`

**Out:** MCP tools, ffmpeg, asset registration.
**Deliverable:** compoziții cu `audio[]` se parsează/validează; tipurile compilează în UI și engine.
**Verificare:** unit tests pe parser + 3 fixture JSON (valid, invalid duration, invalid volume).
**Estimat:** 3-4h.

---

### S2 — Asset registration extins pentru audio
**Scope:**
- `register_asset` acceptă `.mp3 .wav .aac .m4a .ogg`
- Integrare `ffprobe` → extrage `duration`, `sampleRate`, `channels`, `codec`
- Stocare metadata în asset registry
- `list_assets` întoarce metadata audio

**Out:** mux, MCP tools pentru tracks.
**Deliverable:** `register_asset` pe un mp3 returnează metadata; warning dacă lipsește ffprobe.
**Verificare:** test cu 2-3 fișiere audio reale (mp3, wav, m4a).
**Estimat:** 3h.

---

### S3 — MCP tools pentru audio tracks
**Scope:**
- `add_audio_track(asset, start, end?, volume?, fadeIn?, fadeOut?)`
- `update_audio_track(id, ...)`, `remove_audio_track(id)`, `list_audio_tracks()`
- Validări: asset există și e audio; durata track nu depășește durata compoziției (warning, nu eroare — aliniere cu Q6)
- Update doc MCP capabilities

**Out:** mux ffmpeg.
**Deliverable:** pot construi o compoziție cu 2-3 audio tracks via MCP.
**Verificare:** integration test parcurge add → update → list → remove.
**Estimat:** 3-4h.

---

### S4 — Audio mux pipeline post-render
**Scope:**
- Refactor render pipeline din 1 etapă → 2 etape:
  1. rawvideo → `temp_video.mp4` (fără audio, ca azi)
  2. mux: `temp_video.mp4` + N audio tracks → `output.mp4`
- Generator `filter_complex` pentru N tracks: `adelay`, `volume`, `afade` in/out, `amix`
- Resample forțat 48kHz pe toate sursele
- `-c:v copy` la mux (zero re-encode video)
- Cleanup `temp_video.mp4` după mux
- Flag `-movflags +faststart` opțional

**Out:** video items.
**Deliverable:** compoziție cu 1 voiceover + 1 music renderează corect; ffprobe pe output confirmă stream audio cu 48kHz.
**Verificare:** golden test — render fixture, ffprobe output, comparație waveform pe 3 puncte de timp.
**Estimat:** 5-6h.

**Checkpoint la final S4: audio muxing complet funcțional și shipable independent.** Poți pune un tag intermediar `v0.2.0-audio` aici dacă vrei să-l folosești înainte să termini video clips.

---

### S5 — Schema + types: video items
**Scope:**
- Tip nou `VideoItem` (extinde `SpriteItem` conceptual): `asset, x, y, width, height, anchor, start, end?, trimIn?, trimOut?, fit, loop`
- `fit ∈ {cover, contain, fill, none}` (default `contain`)
- DUAL update: engine + `apps/editor/app/types/commands.ts`
- Validări: `0 ≤ trimIn < trimOut ≤ asset.duration`, `end > start`
- **Explicit:** zero câmpuri audio pe `VideoItem`

**Out:** registration, extract, render.
**Deliverable:** compoziții cu video items se parsează; tipuri compilează în UI.
**Verificare:** unit tests parser + fixtures.
**Estimat:** 3h.

---

### S6 — Asset registration extins pentru video
**Scope:**
- `register_asset` acceptă `.mp4 .mov .webm .mkv`
- ffprobe → `duration, width, height, fps, hasAlpha, codec, pixelFormat`
- Warning policy:
  - rezoluție > 4K → warn
  - durată > 60s → warn
  - codec exotic (av1 cu hardware decode lipsă) → warn
- Metadata în registry, queryable

**Out:** pre-extract.
**Deliverable:** register video, warnings apar la limite, metadata corectă.
**Verificare:** test cu 4 video-uri (mic, 4K, lung, webm cu alpha).
**Estimat:** 3-4h.

---

### S7 — Pre-extract pipeline pentru video frames
**Scope:**
- Pre-render phase: pentru fiecare `VideoItem` distinct (asset+trimIn+trimOut+fps+resolution) → spawn ffmpeg:
  ```
  ffmpeg -ss <trimIn> -i <asset> -to <trimOut> -vf "fps=<compFps>,scale=<W>:<H>" cache/<hash>/%05d.png
  ```
- Cache cu hash determinist (asset path + mtime + params)
- Reuse între render-uri și între items care partajează aceleași params
- Progress events către caller
- Cleanup LRU pentru cache (configurabil, default 5GB)

**Out:** drawing la render time.
**Deliverable:** rulează render-ul → pre-extract apare în cache; al doilea render hit cache.
**Verificare:** test cu 1 video, două render-uri consecutive, al doilea trebuie să fie >5x mai rapid.
**Estimat:** 5-6h.

---

### S8 — Render-time drawing pentru video items
**Scope:**
- Renderer recunoaște `type: "video"`
- La frame `f`:
  - `localTime = compTime - item.start`
  - dacă `localTime > (trimOut - trimIn)`:
    - dacă `loop: true` → modulo
    - else → freeze la ultimul frame disponibil
  - `frameIdx = floor(localTime * fps) + 1`
  - `drawImage(cache/<hash>/<frameIdx>.png, ...)` cu transform `fit`
- Implementare `fit`: cover/contain/fill/none cu calcul dst rect
- Tweens existente (x, y, opacity, width, height) funcționează identic ca sprite

**Out:** MCP tools, parallelization.
**Deliverable:** golden render cu video + sprite + text e corect frame-by-frame.
**Verificare:** golden frame comparison la frame-uri cheie: t=0, t=mid, t=end-1, t=după-trim-cu-freeze, t=după-trim-cu-loop.
**Estimat:** 4-5h.

---

### S9 — MCP tools pentru video items
**Scope:**
- `add_video(asset, x, y, width?, height?, start?, end?, trimIn?, trimOut?, fit?, loop?, layer?)`
- `update_video(id, ...)`, integrare cu `remove_item`, `move_item_to_layer`
- Tween-uri compatibile via `add_tween` existent (x, y, opacity, width, height)
- Update `list_engine_capabilities` să raporteze video support
- Update `apps/editor/app/types/commands.ts`

**Out:** UI polish.
**Deliverable:** pot construi end-to-end o compoziție cu video + audio + tweens doar via MCP.
**Verificare:** integration test: scenariu complet add_video → add_tween → render preview frame.
**Estimat:** 3-4h.

---

### S10 — Integration, sample compositions, doc updates
**Scope:**
- 3 sample compositions live în `apps/editor/samples/`:
  - "intro video + voiceover" (1 video full-width + 1 audio)
  - "video cu freeze la final + music bed" (test edge case)
  - "video cu loop + text overlay tween-uit + audio" (combo complex)
- Update `design-doc.md`:
  - §3 (schema) — adaugă `AudioTrack`, `VideoItem`
  - §5 (MCP tools) — adaugă noile tools
  - §6 (ffmpeg pipeline) — pipeline 2 etape + filter_complex
  - §8 (roadmap) — bifează v0.2; clarifică ce a rămas (cubic-bezier, parallelization)
  - Changelog la final
- README quickstart cu un exemplu video+audio

**Out:** test exhaustiv.
**Deliverable:** 3 samples se renderează curat; doc up-to-date.
**Verificare:** rulare manuală a celor 3 samples + lectură doc.
**Estimat:** 3-4h.

---

## Editor UI/UX — sesiuni pentru a expune audio + video și utilizatorilor

Editorul actual (AdonisJS + Inertia + Vue) deja are `Stage.vue`, `Timeline.vue`, `Inspector.vue`, `Library.vue`, `Outliner.vue`. Sesiunile de mai jos extind aceste componente ca utilizatorul să poată **vedea, edita și manipula** audio tracks și video items — paritate funcțională cu MCP-ul.

### Principii UX

- **Audio = time-only.** Nu apare pe Stage. Trăiește exclusiv într-o lane proprie de timeline + Inspector + Outliner secțiune separată.
- **Video = spatial + temporal.** Apare pe Stage (ca sprite), în timeline (ca item cu trim handles), în Outliner (cu iconă proprie).
- **Inspector adaptativ.** Detectează tipul selecției și schimbă panoul (existent deja pentru sprite/text/shape — extindem cu audio + video).
- **Drag-from-Library = primary action.** Drop audio pe timeline → add_audio_track. Drop video pe stage → add_video.
- **Zero re-design global.** Adăugiri incrementale la componente existente, fără refactor major.

---

### U1 — Library: suport audio + video assets
**Scope:**
- `LibraryCard.vue` afișează iconă diferită + durată pentru audio/video
- Filtrare/tab-uri în `Library.vue`: All / Images / Audio / Video
- Hover preview pentru video (primul frame ca thumbnail, generat la register în S6)
- Hover preview pentru audio (waveform mic, generat la register în S2 via ffmpeg `showwavespic`)
- Drag source: emite payload cu tipul assetului (`image | audio | video`) — consumat de Stage/Timeline

**Dependencies:** S2 (audio metadata) + S6 (video metadata).
**Files:** `Library.vue`, `LibraryCard.vue`, `composables/use-library.ts` (eventual nou).
**Deliverable:** library arată audio + video cu thumbnails distincte și filtrare.
**Verificare:** vizual + manual drag test (drop handler fake pentru moment).
**Estimat:** 4-5h.

---

### U2 — Inspector: panou AudioTrack
**Scope:**
- `Inspector.vue` detectează selecția de tip `audio` și randează un sub-panou nou
- Câmpuri editabile cu inputs existente:
  - `asset` (AssetPicker filtrat la audio)
  - `start`, `end` (Time inputs)
  - `volume` (Percent / slider 0-200%)
  - `fadeIn`, `fadeOut` (Time inputs, default 0)
- Action: "Mute" toggle (= volume 0, restorable)
- Validation feedback inline (track depășește durata → warning)
- Două-căi binding cu store-ul de compoziție

**Dependencies:** S1, S3 (MCP tools sunt sursa de modificări — UI le invocă).
**Files:** `Inspector.vue` (sau nou `inspector/AudioInspector.vue`), `inputs/AssetPicker.vue` (filter prop).
**Deliverable:** selectez audio în Outliner → Inspector arată panoul → editezi → modificare ajunge la engine.
**Verificare:** manual + 2 e2e tests (Playwright dacă există, altfel manual checklist).
**Estimat:** 4-5h.

---

### U3 — Timeline: audio lane separată
**Scope:**
- `Timeline.vue` capătă o secțiune "Audio" sub lane-urile vizuale (separator vizual clar)
- `TimelineTrack.vue` reutilizat sau variantă `TimelineAudioTrack.vue` pentru bloc audio:
  - Bloc cu lungimea = `end - start`, poziție = `start`
  - Drag pe X → modifică `start` (cu snap la 0.1s sau frame)
  - Resize handle dreapta → modifică `end`
  - Click → selectează (Inspector arată panoul U2)
  - Right-click → "Mute", "Delete", "Reset volume"
- Indicator vizual pentru mute (gri/striped)
- Indicator vizual pentru fade in/out (gradient la capete)
- Drop target: drag dintr-un audio asset din Library → creează audio track la timpul drop-ului

**Dependencies:** U1 (drag source), U2 (Inspector binding), S3 (MCP).
**Files:** `Timeline.vue`, `TimelineTrack.vue` sau nou `TimelineAudioTrack.vue`.
**Deliverable:** user vede toate audio tracks pe timeline, le poate edita prin drag/resize/drop.
**Verificare:** manual scenariu: drop 2 audio, drag unul, resize altul, mute, undo (dacă există).
**Estimat:** 6-7h.

---

### U4 — Inspector: panou VideoItem
**Scope:**
- Detect selecție tip `video` → sub-panou cu două secțiuni:
  - **Spatial:** `x, y, width, height, anchor, opacity` (identice cu sprite — reuse component)
  - **Source/Time:** `asset` (AssetPicker video), `trimIn`, `trimOut` (Time inputs cu validare ≤ duration), `start`, `end`
  - **Display:** `fit` (Enum: cover/contain/fill/none), `loop` (Boolean)
- Indicator durată calculată: "Source: 12.5s · Visible: 8.0s · Freeze: 0.3s" (sau "Loops: 1.6x" dacă loop)
- Acțiune "Reset trim" → trimIn=0, trimOut=duration
- Preview frame mic în panou (primul frame după trimIn)

**Dependencies:** S5, S6, S9.
**Files:** `Inspector.vue` (sau `inspector/VideoInspector.vue`), `inputs/AssetPicker.vue`.
**Deliverable:** edit complet pe video item prin Inspector.
**Verificare:** manual + edge cases (trim invalid, asset schimbat).
**Estimat:** 5-6h.

---

### U5 — Timeline: video items cu trim handles
**Scope:**
- `TimelineTrack.vue` extins pentru `type: video`:
  - Bloc cu length = `end - start`
  - **Trim handles interne** (stânga/dreapta în interiorul blocului, dincolo de capete) pentru `trimIn`/`trimOut`
  - **Drag exteriorul blocului** → mută `start` (păstrează trim)
  - **Resize capete externe** → mută `end` (extinde freeze sau loop)
  - Indicator vizual:
    - secțiunea de **freeze** = pattern striat la final
    - secțiunea de **loop** = chevron-uri repetate
- Thumbnail strip mini în interiorul bloc-ului (3-5 frame-uri din video, generate din cache S7)
- Drop target Stage: drag video din Library → adaugă video item + segment în timeline

**Dependencies:** U1, U4, S7 (pentru thumbnail strip).
**Files:** `Timeline.vue`, `TimelineTrack.vue`.
**Deliverable:** video items pe timeline complet manipulabile vizual.
**Verificare:** manual scenariu trim + drag + resize + loop visual.
**Estimat:** 7-8h.

---

### U6 — Stage: preview video la playhead curent
**Scope:**
- `Stage.vue` recunoaște `type: video` la randare preview:
  - Calculează frame index curent (`localTime * fps`)
  - Folosește **cache-ul S7** dacă e populat → `drawImage(cache PNG)`
  - Dacă cache lipsește → afișează poster (primul frame extras la register S6) + badge "Pre-extract pending"
  - Loading state vizual când extract rulează
- Tweens spatial (x, y) — aplicate identic cu sprite
- Performance: cache LRU în-memory pe browser (last 30 frames) ca preview-ul să fie smooth la scrub
- Buton "Refresh preview" în StatusBar dacă cache e invalidat

**Dependencies:** S6 (poster), S7 (cache), S8 (drawing logic ca referință).
**Files:** `Stage.vue`, `composables/use-stage-renderer.ts` (sau echivalent existent).
**Deliverable:** scrub playhead în timeline → video preview se actualizează în Stage.
**Verificare:** manual scrub test + măsurare lag (target < 100ms per frame change).
**Estimat:** 6-8h.

---

### U7 — Outliner: audio + video în arborele scenei
**Scope:**
- `Outliner.vue` adaugă **secțiune dedicată "Audio Tracks"** sub layers (separată de visual items, similar cu cum Premiere/After Effects despart)
- `OutlinerNode.vue` capătă iconițe noi:
  - Audio: speaker icon, durată afișată, mute indicator
  - Video: film icon, "▶ 0:00-0:12" range, loop/freeze badge
- Click selectează → Inspector update + highlight pe Stage (video) / Timeline (audio)
- Drag-reorder funcționează identic cu items existenți
- Context menu: Duplicate, Delete, Mute (audio), Reset trim (video)

**Dependencies:** U2, U4.
**Files:** `Outliner.vue`, `OutlinerNode.vue`.
**Deliverable:** outliner arată totul, selecția propagă peste tot.
**Verificare:** manual checklist navigare + selecție bidirecțională (Stage→Outliner, Outliner→Inspector).
**Estimat:** 4-5h.

---

### U8 — Add flow + polish UX
**Scope:**
- **Add menu / palette** extins în `ItemToolbar.vue`:
  - "Add Sprite" (există), "Add Text" (există), "Add Shape" (există)
  - **"Add Video..."** → deschide AssetPicker video, plasează la centrul Stage cu fit=contain
  - **"Add Audio Track..."** → deschide AssetPicker audio, plasează la playhead curent, durata = asset duration
- Keyboard shortcuts: `V` add video, `A` add audio (dacă nu sunt deja luate)
- **Empty state** pentru audio lane: "Drag audio here or click +" hint
- **Status feedback:** toast la add ("Added intro.mp4 at 0:02") via `Toasts.vue`
- **Help overlay** update în `HelpOverlay.vue` cu noile capabilities

**Dependencies:** U1, U3, U5.
**Files:** `ItemToolbar.vue`, `Timeline.vue`, `HelpOverlay.vue`, `Toasts.vue`.
**Deliverable:** user nou poate adăuga primul video + audio fără să citească docs.
**Verificare:** UX walkthrough cu cineva nou (sau auto-test cu Playwright).
**Estimat:** 3-4h.

---

### U9 — Audio waveform în timeline (polish, opțional)
**Scope:**
- Generate waveform thumbnail la `register_asset` (ffmpeg `showwavespic`, PNG cached)
- `TimelineAudioTrack.vue` afișează waveform în interiorul bloc-ului
- Optional: live volume meter în StatusBar la playback

**Dependencies:** U3.
**Files:** asset pipeline (S2), `TimelineAudioTrack.vue`.
**Deliverable:** audio tracks vizibile cu formă de undă reală.
**Verificare:** vizual.
**Estimat:** 3-4h.

---

## Sesiuni opționale (decizie ta dacă rămân în v0.2 sau merg în v0.3)

### S11 (opt) — Cubic-bezier easing
Adaugă `cubic-bezier(x1, y1, x2, y2)` ca string în câmpul `easing` (în plus față de cele named). Solver Newton-Raphson sau De Casteljau. **2-3h.**

### S12 (opt) — Frame-range parallelization
Worker pool: split [0, N) frames în K range-uri, K procese paralele render → K segmente raw → concat. Beneficiu real pe video-uri ≥ 30s. **5-7h.**

Recomandare: dacă vrei v0.2 livrabil rapid, **amână S11+S12 la v0.3**. Audio + video + UI aduc deja valoare imensă singure.

---

## Diagrama dependențelor (engine + UI)

```
Engine/MCP:                          UI/UX:
S1 (audio schema) ───────────────►   U2 (Inspector audio)
                          └──────►   U3 (Timeline audio lane)
S2 (audio assets) ──┐
                    └──────────────► U1 (Library audio + video)
S3 (audio MCP) ─────────────────┐
                                ├──► U2, U3 (UI invoke MCP)
S4 (audio mux) ───────────────► AUDIO MILESTONE ✅

S5 (video schema) ──────────────►   U4 (Inspector video)
                          └─────►   U5 (Timeline video)
S6 (video assets) ──┐
                    └──────────────► U1
S7 (video pre-extract) ─────────►   U6 (Stage video preview)
S8 (video render) ──────────────►   (referință vizuală)
S9 (video MCP) ─────────────────►   U4, U5, U8 (UI invoke MCP)

U7 (Outliner) ◄── U2, U4
U8 (Add flow) ◄── U1, U3, U5
U9 (waveform) ◄── U3 [opțional]

S10 (integration + docs) ◄── totul de mai sus
```

## Wave-uri paralele (3 brațe: engine schema, engine pipeline, UI)

| Wave | Engine schema/MCP | Engine pipeline | UI |
|------|-------------------|-----------------|-----|
| 1 | S1, S5 | — | — |
| 2 | S2, S6 | — | U1 (după S2+S6) |
| 3 | S3, S9 | S7 | U2, U4 (după S1+S3 / S5+S9) |
| 4 | — | S4, S8 | U3, U5, U6 (după S3+S7 / S5+S7+S8) |
| 5 | — | — | U7, U8, U9 |
| 6 | S10 | S10 | S10 |

## Totaluri actualizate

| Categorie | Sesiuni | Ore |
|---|---|---|
| Engine + MCP (core) | S1–S10 | 35-40h |
| UI/UX (core) | U1–U8 | 39-48h |
| UI polish (opțional) | U9 | 3-4h |
| Engine opțional | S11, S12 | 7-10h |
| **Total v0.2 complet** | **18 core + 3 opt** | **74-88h core, ~95-100h cu tot** |

Calendar realist:
- **Solo dev secvențial:** ~4-5 săptămâni
- **Solo dev cu paralelizare pe wave-uri:** ~3 săptămâni
- **2 devs (engine + UI):** ~2 săptămâni

---

## Checkpoint-uri shipable intermediare

1. **După S4:** audio funcționează prin MCP. Tag `v0.2.0-audio-mcp`.
2. **După S4 + U1 + U2 + U3:** audio funcționează și prin UI. Tag `v0.2.0-audio-full`.
3. **După S8 + S9:** video funcționează prin MCP. Tag `v0.2.0-video-mcp`.
4. **După U6 + U7 + U8:** video funcționează și prin UI. Tag `v0.2.0-rc1`.
5. **După S10:** docs + samples. Tag `v0.2.0`.

Fiecare checkpoint e o livrare validă — poți opri la oricare dintre ele și ai un release coerent.
