---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/config/limits.ts
  - src/schemas/index.ts
  - src/types/index.ts
  - src/validators/index.ts
  - src/errors/index.ts
autonomous: true

must_haves:
  truths:
    - "Project can be built with TypeScript"
    - "Video limits are defined in a single location"
    - "Folder structure follows research recommendations"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "zod"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "strict"
    - path: "src/config/limits.ts"
      provides: "Centralized video spec limits"
      exports: ["VIDEO_LIMITS"]
  key_links:
    - from: "package.json"
      to: "tsconfig.json"
      via: "npm scripts reference TypeScript compiler"
      pattern: "tsc"
---

<objective>
Initialize TypeScript project with Zod dependencies and establish the foundational folder structure for video spec validation.

Purpose: Create the project scaffolding and centralized configuration that all subsequent schemas and validators will build upon.
Output: Working TypeScript project with npm scripts, Zod installed, and limits configuration ready for schema use.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize TypeScript project with dependencies</name>
  <files>
    package.json
    tsconfig.json
    .gitignore
  </files>
  <action>
1. Initialize npm project: `npm init -y`

2. Install dependencies:
   ```bash
   npm install zod@^3.24 zod-validation-error@^4
   npm install -D typescript@^5 @types/node@^20 vitest@^2
   ```
   Note: Using Zod 3.24 (latest v3) per research recommendation for ecosystem compatibility.

3. Create tsconfig.json with strict mode:
   ```json
   {
     "compilerOptions": {
       "target": "ES2022",
       "module": "NodeNext",
       "moduleResolution": "NodeNext",
       "lib": ["ES2022"],
       "outDir": "./dist",
       "rootDir": "./src",
       "strict": true,
       "esModuleInterop": true,
       "skipLibCheck": true,
       "forceConsistentCasingInFileNames": true,
       "declaration": true,
       "declarationMap": true,
       "sourceMap": true,
       "resolveJsonModule": true
     },
     "include": ["src/**/*"],
     "exclude": ["node_modules", "dist", "tests"]
   }
   ```

4. Update package.json scripts:
   ```json
   {
     "scripts": {
       "build": "tsc",
       "test": "vitest run",
       "test:watch": "vitest",
       "typecheck": "tsc --noEmit"
     },
     "type": "module"
   }
   ```

5. Add .gitignore entries for node_modules, dist, coverage if not present.
  </action>
  <verify>
    - `npm run typecheck` exits 0
    - `npm run build` creates dist/ folder
    - `npm ls zod` shows zod@3.24.x
  </verify>
  <done>
    TypeScript compiles successfully, Zod is installed, npm scripts work.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create folder structure and limits configuration</name>
  <files>
    src/config/limits.ts
    src/schemas/index.ts
    src/types/index.ts
    src/validators/index.ts
    src/errors/index.ts
    src/index.ts
  </files>
  <action>
1. Create src/ folder structure per research recommendations:
   ```
   src/
   ├── config/
   │   └── limits.ts
   ├── schemas/
   │   └── index.ts
   ├── types/
   │   └── index.ts
   ├── validators/
   │   └── index.ts
   ├── errors/
   │   └── index.ts
   └── index.ts
   ```

2. Create src/config/limits.ts with VIDEO_LIMITS constant:
   ```typescript
   /**
    * Video specification limits.
    * All constraints are validated against these values.
    * Centralized here to allow easy adjustment without touching schemas.
    */
   export const VIDEO_LIMITS = {
     /** Maximum width in pixels */
     maxWidth: 1920,
     /** Maximum height in pixels */
     maxHeight: 1920,
     /** Maximum frames per second */
     maxFps: 60,
     /** Minimum frames per second */
     minFps: 1,
     /** Default frames per second when not specified */
     defaultFps: 30,
     /** Maximum video duration in seconds */
     maxDuration: 300,
   } as const;

   /** Type for VIDEO_LIMITS for use in other modules */
   export type VideoLimits = typeof VIDEO_LIMITS;
   ```

3. Create placeholder index.ts files for each module:
   - src/schemas/index.ts: `// Video spec schemas - implemented in Plan 02`
   - src/types/index.ts: `// TypeScript types inferred from Zod schemas`
   - src/validators/index.ts: `// Validation functions - implemented in Plan 02`
   - src/errors/index.ts: `// Error formatting utilities`

4. Create src/index.ts barrel export:
   ```typescript
   export * from './config/limits.js';
   // Other exports added as modules are implemented
   ```
  </action>
  <verify>
    - `npm run typecheck` passes
    - `npm run build` succeeds
    - dist/config/limits.js exists
    - Importing VIDEO_LIMITS works: `node -e "import('./dist/config/limits.js').then(m => console.log(m.VIDEO_LIMITS))"`
  </verify>
  <done>
    Folder structure exists, VIDEO_LIMITS is exported and usable, project builds cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` - no TypeScript errors
2. `npm run build` - creates dist/ with compiled JS
3. VIDEO_LIMITS values match requirements:
   - maxWidth: 1920, maxHeight: 1920 (OUTP-02)
   - minFps: 1, maxFps: 60, defaultFps: 30 (OUTP-03)
   - maxDuration: 300 (OUTP-04)
</verification>

<success_criteria>
- TypeScript project compiles with strict mode
- Zod 3.24.x and zod-validation-error installed
- VIDEO_LIMITS constant matches all phase requirements
- Folder structure ready for schema implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
