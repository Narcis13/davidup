---
phase: 06-ai-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/services/ai-client.ts
  - src/schemas/template.ts
  - src/api/types.ts
autonomous: true
user_setup:
  - service: OpenRouter
    why: "AI model access for template generation"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter Dashboard -> API Keys"
    dashboard_config:
      - task: "Create account and generate API key"
        location: "https://openrouter.ai/keys"

must_haves:
  truths:
    - "OpenRouter client can call AI API and return structured response"
    - "Template schemas validate generate requests and responses"
    - "Platform presets provide correct dimensions for TikTok/YouTube/Instagram"
  artifacts:
    - path: "src/api/services/ai-client.ts"
      provides: "OpenRouter API client with retry and error handling"
      exports: ["callOpenRouter", "OpenRouterMessage", "OpenRouterRequest", "OpenRouterResponse"]
    - path: "src/schemas/template.ts"
      provides: "Zod schemas for template generation and built-in templates"
      exports: ["GenerateRequestSchema", "GenerateResponseSchema", "BuiltInTemplateSchema", "PLATFORM_PRESETS"]
    - path: "src/api/types.ts"
      provides: "Extended types for template features"
      contains: "GenerateRequest"
  key_links:
    - from: "src/api/services/ai-client.ts"
      to: "https://openrouter.ai/api/v1"
      via: "fetch with retry"
      pattern: "openrouter\\.ai"
---

<objective>
Create OpenRouter AI client and template schemas for Phase 6 AI integration.

Purpose: Establish foundation for AI-powered template generation with proper typing, validation, and API communication.
Output: OpenRouter client service and Zod schemas for generate requests/responses.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-integration/06-RESEARCH.md
@src/api/types.ts
@src/api/services/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create template schemas</name>
  <files>src/schemas/template.ts</files>
  <action>
Create Zod schemas for template generation:

1. PLATFORM_PRESETS const with dimensions:
   - tiktok: { width: 1080, height: 1920, fps: 30 }
   - youtube: { width: 1920, height: 1080, fps: 30 }
   - instagram: { width: 1080, height: 1080, fps: 30 }

2. GenerateRequestSchema:
   - description: z.string().min(10).max(1000)
   - platform: z.enum(['tiktok', 'youtube', 'instagram'])
   - style: z.enum(['energetic', 'professional', 'playful'])

3. TemplateVariableSchema:
   - name: z.string()
   - description: z.string().optional()
   - type: z.enum(['text', 'url', 'color'])
   - default: z.string().optional()

4. GenerateResponseSchema:
   - spec: VideoSpecSchema (import from video-spec.ts)
   - variables: z.array(TemplateVariableSchema)

5. BuiltInTemplateSchema:
   - id: z.string()
   - name: z.string()
   - description: z.string()
   - platform: z.enum(['tiktok', 'youtube', 'instagram', 'universal'])
   - style: z.string()
   - variables: z.array(TemplateVariableSchema)
   - spec: VideoSpecSchema

Export inferred types: GenerateRequest, GenerateResponse, TemplateVariable, BuiltInTemplate
  </action>
  <verify>npx tsc --noEmit src/schemas/template.ts</verify>
  <done>Template schemas defined with proper Zod types, PLATFORM_PRESETS exported</done>
</task>

<task type="auto">
  <name>Task 2: Create OpenRouter AI client</name>
  <files>src/api/services/ai-client.ts</files>
  <action>
Create OpenRouter API client with retry and error handling:

1. Define interfaces:
   - OpenRouterMessage: { role: 'system' | 'user' | 'assistant', content: string }
   - OpenRouterRequest: { model: string, messages: OpenRouterMessage[], response_format?: { type: 'json_object' }, temperature?: number, max_tokens?: number }
   - OpenRouterResponse: { id: string, choices: Array<{ message: { role: string, content: string }, finish_reason: string }>, usage: { prompt_tokens: number, completion_tokens: number, total_tokens: number } }

2. Implement callOpenRouter function:
   - Check OPENROUTER_API_KEY env var, throw descriptive error if missing
   - Use async-retry (already in deps) with { retries: 2, factor: 2, minTimeout: 1000 }
   - Set headers: Authorization Bearer, Content-Type, HTTP-Referer (APP_URL or 'https://gamemotion.dev'), X-Title: 'GameMotion'
   - Use AbortSignal.timeout(OPENROUTER_TIMEOUT || 60000)
   - Handle non-ok responses with detailed error messages
   - Return typed OpenRouterResponse

3. Export DEFAULT_MODEL constant: process.env.OPENROUTER_MODEL || 'anthropic/claude-sonnet-4'

Use native fetch (no axios). Follow existing service patterns from job-store.ts and api-key-store.ts.
  </action>
  <verify>npx tsc --noEmit src/api/services/ai-client.ts</verify>
  <done>OpenRouter client handles API calls with retry, timeout, and proper error messages</done>
</task>

<task type="auto">
  <name>Task 3: Extend API types and exports</name>
  <files>src/api/types.ts, src/api/services/index.ts, src/schemas/index.ts</files>
  <action>
1. In src/api/types.ts:
   - Re-export GenerateRequest, GenerateResponse, TemplateVariable, BuiltInTemplate from schemas/template.ts
   - Add type alias: Platform = 'tiktok' | 'youtube' | 'instagram'
   - Add type alias: Style = 'energetic' | 'professional' | 'playful'

2. In src/api/services/index.ts:
   - Add export for ai-client: export { callOpenRouter, DEFAULT_MODEL, type OpenRouterMessage, type OpenRouterRequest, type OpenRouterResponse } from './ai-client.js'

3. In src/schemas/index.ts:
   - Add export for template schemas: export * from './template.js'
  </action>
  <verify>npx tsc --noEmit && npm test -- --grep "types" --passWithNoTests</verify>
  <done>Template types exported from API types, AI client exported from services</done>
</task>

</tasks>

<verification>
1. Run type check: `npx tsc --noEmit`
2. Verify imports work: Create test file that imports from api/types and api/services
3. Check PLATFORM_PRESETS has correct dimensions for each platform
</verification>

<success_criteria>
- [ ] src/schemas/template.ts exports GenerateRequestSchema, GenerateResponseSchema, BuiltInTemplateSchema, PLATFORM_PRESETS
- [ ] src/api/services/ai-client.ts exports callOpenRouter, DEFAULT_MODEL
- [ ] All types re-exported from src/api/types.ts
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-integration/06-01-SUMMARY.md`
</output>
